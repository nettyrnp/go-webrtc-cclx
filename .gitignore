# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

gin-bin

# Ssl  keys
signaler/localhost.pem
signaler/localhost.key
www/localhost.pem
www/localhost.key
